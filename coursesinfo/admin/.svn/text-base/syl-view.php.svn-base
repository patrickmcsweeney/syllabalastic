<?php


require_once('include.php');

$current_user = $_SERVER["REMOTE_USER"];

$id = preg_replace( '/[^0-9]/', '', $_GET["id"] );

# throws an exception if no syllabus found
$syllabus = new UoS_Syllabus( $id );

renderHeader( $syllabus->title() );
print $syllabus->render();
renderFooter();


exit;


